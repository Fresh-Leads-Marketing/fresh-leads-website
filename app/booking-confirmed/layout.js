export const metadata = {
  title: "Booking Confirmed | Fresh Leads Marketing",
  description: "Your free marketing audit has been booked. Here's what to expect before your call with Fresh Leads Marketing.",
  robots: { index: false, follow: false },
  alternates: {
    canonical: "https://freshleadsmarketing.com/booking-confirmed",
  },
};

export default function Layout({ children }) {
  return children;
}
