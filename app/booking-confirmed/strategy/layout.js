export const metadata = {
  title: "Strategy Call Booked | Fresh Leads Marketing",
  description: "Your monthly strategy call has been booked. Here's what to expect.",
  robots: { index: false, follow: false },
  alternates: {
    canonical: "https://freshleadsmarketing.com/booking-confirmed/strategy",
  },
};

export default function Layout({ children }) {
  return children;
}